function switchDE() {
    document.getElementById (`secondPhrase`) .innerHTML = (`Die beste Suppe in der Stadt`);
    document.getElementById (`order`) .innerHTML = (`Wie man bestellt`);
    document.getElementById (`noodle`).innerHTML =(`Wähle die Nudeln)`);
    document.getElementById (`broth`).innerHTML =(`Wähle deine Beilage`);
    document.getelementById (`scrambles`).innerHTML = (`Wähle dein Ei`);
    document.getelementById (`menu`).inerHTML = (`Menü`);
    document.getelementById (`chicken`).inerHTML = (`Hühnchen`);
    document.getelementById (`Rind`).inerHTML = (`Rind`);
    document.getelementById (`schrimps`).inerHTML = (`Schrimps`);
}
function switchEN() {
    document.getElementById('secondPhrase') .innerHTML =`The best Ramen in Town`;
    document.getElementById (`order`) .innerHTML = (`How to Order`);
    document.getElementById (`noodle`).innerHTML =(`Pick ur Noodle`);
    document.getElementById (`broth`).innerHTML =(`Pick ur Broth`);
    document.getelementById (`scrambles`).innerHTML = (`Pick ur Egg`);
    document.getelementById (`menu`).inerHTML = (`Menü`);
    document.getelementById (`chicken`).inerHTML = (`Chicken`);
    document.getelementById (`Rind`).inerHTML = (`Beef`);
    document.getelementById (`schrimps`).inerHTML = (`Shrimp`);
}