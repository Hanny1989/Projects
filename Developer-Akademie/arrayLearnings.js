let myList =[ "snips", "banana" , true]; //array wird immer in eckigen klammern gschrieben 

let fruits = ["Banana", "Orange", "Apple", "Mango"];

    
let singletestVar  = "asd";


function initArray(){
    console.log(singletestVar);

}

let myList =[ "snips", "banana" , true]; //array wird immer in eckigen klammern gschrieben 

let fruits = ["Banana", "Orange", "Apple", "Mango"];

    
let singletestVar  = "asd";

function containsElement(array, elemnt){
    return array.includes(elemnt);  // return gibt das folgende array aus und setzt es auf true or false 

}


function initArray(){
    console.log(containsElement(fruits, "Mango"));
}

function initArray(){
    for (let index = 0; index < array.length; index++) {
        const element = array[index];
        
    }// für was // (start = 0  //definierte variable einfügen; solange ist kleiner als 3 <3 // boolean wie lange ; schritt // zum abbrechen des boolean = element++) {
      
        
    }



// start = let definiert das elemnt das genutzt werden soll , das boolean regelt die max anzahl und der schritt wertet die schritte aus also ob in 1er,2er,3er schirtte ect 



function initArray(){
    let contenRef = document.getElementById(1'myContent');
    5contenRef.innerHTML = "";
    for (2let indexFruits = 0; 3indexFruits < fruits.length; 4indexFruits++) {
      5contenRef.innerHTML += 6`<p> ${fruits[indexFruits]} </p>`
        
    }

1wir greifen normal mit document xx auf die id aus dem html zu 
2,3,4 wir nutzen wieder eine for function mit start, länge undart der aufzählung 
5+5 "neu" wir nutzen contenRef.innerhtml um der liste mehr möglichkeiten zu geben. 
6 ist eine möglichkeit die function 5 zu nutzen. styletags sind im innerhtml css bar 


// NaN = Not a Number! wichtig für schleifen !


// wieder einsetzen 
/*
let myList =[ "snips", "banana" , true]; //array wird immer in eckigen klammern gschrieben 

let fruits = ["Banana", "Orange", "Apple", "Mango"];

    
let singletestVar  = "asd";

function containsElement(array, elemnt){
    return array.includes(elemnt);

}


function initArray(){
    let contenRef = document.getElementById('myContent');
    contenRef.innerHTML = "";
    for (let indexFruits = 0; indexFruits < fruits.length; indexFruits++) {
      contenRef.innerHTML += `<p> ${fruits[indexFruits]} </p>`
        
    }

}*/